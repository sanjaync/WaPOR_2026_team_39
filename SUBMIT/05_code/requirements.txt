# Core pipeline
geopandas>=1.0
rasterio>=1.3
shapely>=2.0
pyproj>=3.6
numpy>=1.26
pandas>=2.0

# Step 02 only - downloads WaPOR v3 rasters from FAO.
# Needs the GDAL C library first (see docs/DATA_SOURCES.md); if it will not
# install, use the manual download fallbacks documented there.
# wapordl>=1.2
